# 工程師交接：控制面板與實境監控（2026-04-11）

## 本次重點

- 已補齊側欄「智能體對話」送出後的雙路回覆顯示：
  - Webview 面板：`agentReplyPanel`
  - 終端機：`postTerminalLog` → VS Code `智能體回覆終端機`
- 實境面板升級為「漸層角色徽章 + 狀態燈 + 任務負載 KPI」：
  - 每個智能體顯示 `任務數 / 執行中 / 待處理 / 阻塞 / 最近更新`
  - `running/pending/failed/idle` 對應狀態燈與卡片色
- 追加語言監控卡片：
  - 接 `/system/language-monitoring`
  - 顯示樣本可用性、事件數、關鍵監控指標（意圖分布、理解分數、關鍵詞回覆率、漂移風險、資料缺口）
- 實境面板輪詢頻率由 65 秒調整為 15 秒（即時性提升）
- API 權限韌性：若匿名請求 401/403，會自動 fallback 改用 token 再取一次
- 主系統監控頁（`/.sync_user_project/templates/chat.html`）Agents 分頁同步升級：
  - 顯示漸層頭像徽章、狀態標籤、每智能體任務負載 KPI
  - 監控資料流新增 `/agent/tasks?status=all&limit=200`
  - 總覽新增「任務監控（未結案/總任務）」卡片
- 修復主頁樣式來源 404：
  - 新增 `.sync_user_project/static/css/visual_template_clean.css`
  - `/static/css/visual_template_clean.css` 由 404 轉為 200

## 主要檔案

- `cursor-agent-sidebar-extension/extension.js`
- `cursor-agent-sidebar-extension/media/sidebar.js`
- `docs/智能體功能對應圖.md`
- `.sync_user_project/templates/chat.html`
- `.sync_user_project/static/css/visual_template_clean.css`

## 驗收步驟

1. 重新部署擴充（若尚未部署）：`bash tools/install_cursor_agent_sidebar.sh`
2. 重新載入 Cursor 視窗（Reload Window）後打開智能體側欄。
3. 在「智能體對話」送出訊息，確認：
   - 面板內看到回覆文字
   - 新開的 `智能體回覆終端機` 同步看到回覆
4. 觀察「實境面板」：
   - 每位智能體有馬賽克頭像
   - 卡片顯示狀態燈與 KPI
   - 狀態變更時 15 秒內刷新
5. 確認「語言監控指標」卡片有資料（若樣本不足會顯示警示，不視為錯誤）。

## 風險與備註

- 開發機若無 `node`，無法本機執行 JS 語法檢查；建議在有 Node 的環境跑一次 lint/check。
- 若後端 API 開啟強制 token，請在側欄正確填入 `SERVER_API_TOKEN`。
